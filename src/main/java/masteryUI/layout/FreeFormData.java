package masteryUI.layout;

/**
 * Contains the layout information for a specific ui element in a free form layout. No Information because the position is managed by the
 * elements itself.
 *
 * @author Subaro
 */
public class FreeFormData extends LayoutData {
    public static FreeFormData DEFAULT = new FreeFormData();
}
